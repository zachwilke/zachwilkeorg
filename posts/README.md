Add a post: create `posts/your-slug.md` with `title` and `date` (ISO) in the `---` front matter (`summary` is optional).
Add `"your-slug"` to the array in `posts/index.json`.
Add `https://zachwilke.org/blog/post.html?p=your-slug` to `sitemap.xml`.
Newest posts are listed first from the date field.
Delete `hello.md` and its index/sitemap lines when you write a real first post.
